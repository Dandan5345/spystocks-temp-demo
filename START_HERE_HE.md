# מערכת בדיקת מידע — הוראות הפעלה

## הפעלה מהירה ב־Mac

1. פתחי את התיקייה `information-check-system`.
2. לחצי פעמיים על `start.command`.
3. בהפעלה הראשונה המחשב יתקין את חבילות Python. לאחר מכן הדפדפן ייפתח בכתובת `http://127.0.0.1:8000`.
4. משאירים את חלון הטרמינל פתוח בזמן השימוש. לסיום לוחצים `Control+C`.

אם macOS חוסם את הקובץ: לחיצה ימנית על `start.command`, לבחור **Open**, ואז לאשר.

## הפעלה ידנית

נדרש Python 3.11 ומעלה. מתוך Terminal:

```bash
cd information-check-system
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python run.py
```

לאחר מכן פותחים `http://127.0.0.1:8000`.

## מה כלול

- חיפוש אינסיידרים ותקצירי פעילות מתוך מאגר SEC מקומי ומהיר.
- פוליטיקאים, אנשי הממשל והקשרים משפחתיים פומביים.
- מוסדיים, נתוני 13F, אחזקות ומידע כללי.
- מצב בהיר, כהה או התאמה למערכת.
- בסיסי הנתונים וה־snapshots שהוכנו לדמו.

## אבטחה

החבילה כוללת קובץ `.env` פרטי עם הגדרות גישה. אין להעלות אותה ל־GitHub ציבורי, ל־Drive ציבורי או להעביר לאנשים נוספים. אם החבילה נחשפת, יש לבטל ולהחליף את מפתח ה־API.

המערכת מיועדת לבדיקה בלבד. הנתונים מגיעים ממקורות ציבוריים רשמיים ומ־Wikipedia ואינם ייעוץ השקעות.
